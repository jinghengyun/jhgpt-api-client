# GitHub 发布教程 —— 从零到上线

本教程教你如何把写好的 README.md 变成一个正式的 GitHub 仓库，让全球开发者都能搜到。

---

## 前提准备

你需要装两个东西（如果没有的话）：

### 1. 检查 Git 是否已安装

打开 PowerShell，运行：

```powershell
git --version
```

如果输出版本号（比如 `git version 2.45.0`），说明已安装，跳过下一步。

如果提示"不是内部或外部命令"，需要安装 Git：

1. 打开 [https://git-scm.com/download/win](https://git-scm.com/download/win)
2. 下载 Windows 版本，一路点「Next」安装
3. 安装完成后**重新打开 PowerShell**，再试一次 `git --version`

### 2. 注册 GitHub 账号

1. 打开 [https://github.com/](https://github.com/)
2. 点击右上角「Sign up」
3. 填写邮箱、密码、用户名（用户名会出现在仓库地址里，建议用 `jinghengyun` 或和你品牌一致的名字）
4. 验证邮箱后登录

---

## 第一步：配置 Git 用户名和邮箱

GitHub 需要知道是谁提交的代码。在 PowerShell 中运行：

```powershell
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的GitHub注册邮箱"
```

验证配置：

```powershell
git config --global user.name
git config --global user.email
```

---

## 第二步：创建本地 Git 仓库

打开 PowerShell，进入刚才写好的文件所在目录：

```powershell
cd "E:\workbuddy数据\2026-06-05-10-25-36\github-ad"
```

初始化 Git 仓库：

```powershell
git init
```

输出 `Initialized empty Git repository in ...` 就表示成功了。

把 README.md 添加到暂存区：

```powershell
git add README.md
```

创建第一次提交：

```powershell
git commit -m "feat: 初始化 jhgpt-api-client 项目"
```

输出类似 `[main (root-commit) abc1234] feat: 初始化...` 表示提交成功。

---

## 第三步：在 GitHub 上创建远程仓库

1. 登录 GitHub，点击右上角的 **「+」→「New repository」**
2. 填写以下信息：

| 字段 | 填什么 |
|------|--------|
| Repository name | `jhgpt-api-client`（建议，用英文小写+连字符） |
| Description | 国内可直连的 Claude/GPT/Gemini API 客户端工具包 |
| Visibility | 🔴 **Public**（公开，这样搜索引擎能搜到） |
| Initialize this repository with | ❌ **什么都不要勾选！** |

3. 点击绿色的「**Create repository**」按钮

4. 创建完后会跳转到一个引导页面，复制第二段命令（"push an existing repository"），类似：

```bash
git remote add origin https://github.com/你的用户名/jhgpt-api-client.git
git branch -M main
git push -u origin main
```

---

## 第四步：推送代码到 GitHub

回到 PowerShell，粘贴刚才复制的命令逐条执行：

```powershell
git remote add origin https://github.com/你的用户名/jhgpt-api-client.git
```

```powershell
git branch -M main
```

```powershell
git push -u origin main
```

第一次推送会弹出一个 GitHub 登录窗口，点击「Sign in with your browser」或输入用户名密码完成认证。

推送成功后，刷新 GitHub 页面，就能看到你的 README 了。

---

## 第五步：优化仓库信息（提高曝光）

回到 GitHub 仓库页面，按以下顺序操作：

### 5.1 添加 Topics（标签）

在仓库页面右侧找到「**About**」区域，点击齿轮图标 ⚙️：

添加以下 topics（每输入一个按回车）：

```
api-client openai chatgpt claude gemini ai deepseek llm python stable-api china
```

这些标签会帮助开发者在 GitHub 搜索时找到你的项目。

### 5.2 开启 Discussions（讨论区）

在仓库顶部导航栏找到「**Settings**」→ 左侧找到「**General**」→ 滚动到「**Features**」区域 → 勾选「**Discussions**」。

这样开发者可以在你的仓库里提问交流，增加互动。

### 5.3 设置仓库网站链接

在「About」区域的齿轮里，把「Website」填上：

```
https://api.jhgpt.com/
```

---

## 第六步：验证效果

1. **搜索测试**：在 GitHub 搜索 `openai compatible api china` 或 `claude gpt gemini api client`，看看能否搜到你的仓库（有几分钟索引延迟，正常）
2. **Badge 测试**：README 里的徽章应该会自动渲染
3. **链接测试**：点击 README 中的链接，确认都能正常跳转

---

## 后续维护建议

| 频率 | 做什么 |
|------|--------|
| 有新模型上线时 | 更新 README 中的模型列表表格 |
| 每周 | 查看 Issues / Discussions，回复用户问题 |
| 每月 | 在 Discussions 发布一条更新日志 |
| 持续 | 别人给你的仓库 Star 越多，GitHub 搜索排名越高 |

---

## 进阶：配置 GitHub Pages（可选）

如果你想要一个更正式的官方文档站：

1. 在仓库里新建 `docs/` 目录，放 HTML 或 Markdown 文件
2. Settings → Pages → Source 选 `Deploy from a branch` → 分支选 `main` → 文件夹选 `/docs`
3. 保存后等一两分钟，访问 `https://你的用户名.github.io/jhgpt-api-client/` 就能看到

---

## 常见问题

**Q: 推送时报错 `remote origin already exists`？**

```powershell
git remote remove origin
git remote add origin https://github.com/你的用户名/jhgpt-api-client.git
git push -u origin main
```

**Q: 推送时提示需要登录？**

2021 年之后 GitHub 不支持密码登录，需要用 Personal Access Token 或 GitHub CLI。

方法一（推荐）：安装 GitHub CLI：

```powershell
winget install --id GitHub.cli
```

安装后运行：

```powershell
gh auth login
```

选择 GitHub.com → HTTPS → Login with a web browser，按提示操作。

方法二：创建 Personal Access Token：

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token → 勾选 `repo` 权限 → 生成后复制 token
3. 推送时用户名填 GitHub 用户名，密码填 token

---

完成！有问题随时问。
